# menu_block_split
a drupal module to split a menu tree over two blocks
With this module you can split any menu block into two different blocks: 
a first block with the first level menu entries only and a second block 
with any second level and sub level menu entries. 
You can have as many splitted blocks as you need.
